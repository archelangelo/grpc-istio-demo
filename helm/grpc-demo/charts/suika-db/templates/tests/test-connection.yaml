apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "suika-db.fullname" . }}-test-connection"
  labels:
{{ include "suika-db.labels" . | indent 4 }}
  annotations:
    "helm.sh/hook": test-success
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args:  ['{{ include "suika-db.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
