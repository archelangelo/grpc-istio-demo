apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "grpc-demo.fullname" . }}-test-connection"
  labels:
{{ include "grpc-demo.labels" . | indent 4 }}
  annotations:
    "helm.sh/hook": test-success
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args:  ['{{ include "grpc-demo.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never